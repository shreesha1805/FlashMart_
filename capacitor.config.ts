import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.swiftdashmerch',
  appName: 'swift-dash-merch',
  webDir: 'dist'
};

export default config;
